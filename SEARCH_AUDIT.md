# SEARCH_AUDIT — 검색 시스템 전면 감사 및 재설계

> 목표: EJU **일본어 · 수학 · 종합과목** 전체를 자연어로 검색. 입력 시 **관련 문제 · 출제년도 · 정답률 · 관련 개념** 제공.
> 원칙: 코드·실데이터로 검증. **허위 수치·허위 결과 생성 금지.**

---

## 1. 현재 검색 시스템 감사 (실측)

저장소에 검색 구현이 **3개 분산**되어 있고, 어느 것도 자연어 통합 검색으로 노출되지 않았다.

| 위치 | 방식 | 치명적 문제 (코드 근거) |
|------|------|------------------------|
| `src/vector/embeddingStore.js` | "임베딩" = **162개 한국어 단어** 고정 lexicon TF-BoW, 전수 cosine | **언어 불일치(치명).** 코퍼스는 일본어 OCR인데 lexicon이 한국어 단어(`수요`,`환율`,`헌법`…)뿐. 일본어 질의 `ブレトンウッズ`나 한국어 `브레튼우즈`(lexicon에 없음) → 벡터 0 → **결과 0**. `buildEjuLexicon()` L51 참조 |
| `src/pipeline/similaritySearch.js` | CJK 문자+bigram/trigram **TF cosine** | **IDF 없음**(흔한 글자 과대평가), 인덱스 없는 O(N×토큰) 전수 비교, 질의 문자열엔 domain/topic이 없어 부스트 무력화. UI 미연결 |
| `src/admin/pages/Vector.jsx` (직전 작업) | `computeEmbedding`(위 한국어 lexicon) 재사용 | 동일한 언어 불일치 결함 상속 |

**요약:** 핵심 결함은 **교차언어(한국어/혼합 질의 ↔ 일본어 코퍼스)** 를 잇는 다리가 전혀 없다는 것. 예시 질의 4종이 현재 시스템에서 사실상 0건을 반환한다.

## 2. 데이터 현실 (감사로 드러난 제약 — 지어내지 않음)

| 과목 | 실데이터 | 비고 |
|------|----------|------|
| 종합과목 | **840문항** (2002–2015, public 28파일) | `keywords` 61%, `topic` 43%, `concepts` **3.6%**(거의 빔) |
| 수학 | **646문항** (2005–2025, 38파일) — *이번에 public으로 복사* | `topic` 42%, `concepts` **0%**. OCR 본문은 수식 위주라 한자 토큰 희박(`内積`·`微分`·`行列` = 0파일) → **topic 라벨(한국어)이 주 신호** |
| 일본어 | **0문항** | 기출 문제 코퍼스 자체가 저장소에 없음 (점수분석·OCR 최적화 코드만) |

추가 제약:
- **정답률(공식 문항별 정답률) 데이터 없음.** 어떤 데이터셋에도 존재하지 않음. 존재하는 것은 `difficulty`(1–10, OCR 파이프라인 추정)와 사용자 본인 오답이력뿐.
- **관련 개념**은 비어있는 `concepts` 대신 `keywords`+`topic`+지식그래프에서 도출해야 함.

## 3. 개선안 — 구현 완료 (오프라인 즉시 동작)

### 3.1 교차언어 개념 브릿지 (핵심 수정)
`src/admin/lib/conceptBridge.js` — 한국어/영어 질의를 **일본어 표면형 + 정규 개념 라벨**로 확장하는 실제 사전(경제·정치·역사·지리·사회·수학). 예:
- `브레튼우즈 체제` → `ブレトンウッズ / 金本位 / 固定相場制 / IMF`
- `국제연합 헌장 초안 회의` → `国際連合 / 国連憲章 / サンフランシスコ会議`
- `벡터 내적` → `ベクトル / 内積 / 成分`

### 3.2 BM25 + CJK n-gram 인덱스 (구 TF-BoW 대체)
`src/admin/lib/searchEngine.js` — 일본어 본문을 문자 bigram/trigram + 가나/한자 런 + 라틴 토큰으로 토큰화하고 **IDF 가중(BM25, k1=1.4,b=0.75)**. `topic`(한국어 라벨)·`keywords`를 함께 색인 → 수학처럼 본문이 빈약해도 topic으로 매칭. domain/concept 부스트는 투명하게 소폭(×1.12/×1.5).

### 3.3 정답률의 정직한 처리 (사용자 선택 반영)
`src/admin/lib/personalAccuracy.js` — 공식 정답률이 없으므로 **만들지 않음**. 대신:
- **추정 난이도** `difficulty/10` (라벨: 쉬움/보통/어려움)
- **내 정답률** — 기존 검증된 `buildPersonalWeaknessGraph`(내 오답이력) 재사용. 사용자가 실제 응시한 topic만 표시, 없으면 "데이터 없음 · 공식 정답률 미제공" 명시.

### 3.4 결과 구성 (요구 4종 충족)
검색 결과 카드마다: **관련 문제**(본문·과목·연·회·문항·관련도), **출제년도**(우측 facet에 결과 연도 분포 + topic별 실제 출제연도), **정답률**(추정난이도+내정답률), **관련 개념**(keywords+topic+브릿지+동일영역 빈출 topic).

### 3.5 의미검색 강화 (옵션 토글)
`src/admin/lib/embeddings.js` — 토글 시 `transformers.js`로 **실제 다국어 임베딩**(`multilingual-e5-small`, 384d) 로드 → query/passage 임베딩 → cosine을 BM25와 블렌딩(가중 0.45). 기본은 꺼짐(모델 수백 MB 다운로드는 UI에 명시). 이 벡터는 프로덕션 pgvector와 동일 표현.

### 3.6 과목 범위
`#/admin/search` 에 3과목 탭. 종합+수학 실검색. **일본어는 0건 + "코퍼스 준비중" 안내**(코퍼스 적재 시 `search_manifest.json` 자동 연결).

## 4. Postgres + pgvector 재설계 (프로덕션 타깃)

전체 SQL: **[`db/search/search.sql`](db/search/search.sql)**. 요지:
- `question_embeddings vector(384)` + **HNSW**(`vector_cosine_ops`) — 전수 cosine(구 방식) 제거, 수십만 행 확장.
- 렉시컬: `tsvector(GIN)` + **`pg_bigm`**(CJK bigram) — 일본어 형태소 경계 보완.
- 필터: `(subject, exam_year, exam_round)` 복합 인덱스 (연도별/과목별).
- `topic_years` 머티리얼라이즈드 뷰 → **출제년도** 집계.
- `search_questions(...)` **하이브리드 RPC**: 필터 → (벡터 ⊕ 렉시컬 가중합) → 문항+출제년도+난이도+관련개념 반환. 공식 정답률은 미존재이므로 반환 안 함(내 정답률은 클라이언트 조인). 임베딩 모델은 인앱 옵션과 동일(e5-small) → 벡터 호환.

**전환 비용:** 클라이언트는 `searchData/searchEngine` 호출을 `rpc('search_questions', …)` 로 교체하면 끝. UI·결과 스키마 동일.

## 5. 검증 (실데이터)

- `src/test/searchEngine.test.js` — **실제 public 코퍼스** 로드 후 예시 질의 실행. **10/10 PASS.**
  - `벡터 내적`·`삼각함수`·`확률 기댓값` → 실제 문항 매칭(한국어 질의 ↔ 일본어/한국어 토픽).
  - `행렬 문제` → **개념은 인식하되 결과 0** = 정직(EJU 수학 교육과정에 행렬 미출제, `行列` 0파일 확인). UI가 "개념 인식했으나 코퍼스에 없음"으로 설명.
  - 종합 경제 질의 브릿지 매칭, 빈 질의 0건.
- 빌드 PASS, 전체 테스트 PASS (§아래 최종 보고).

## 6. 남은 한계 (정직)

1. **일본어 검색 불가** — 코퍼스 부재. 파이프라인만 준비.
2. **공식 정답률 부재** — 추정난이도+내정답률로 대체, 공식값은 데이터 유입 전까지 미제공.
3. **종합 본문 OCR 노이즈** — 초기 연도 깨진 텍스트가 렉시컬 재현율을 일부 제한. 의미검색 모델 토글로 완화 가능.
4. **수학 본문 빈약** — topic 라벨 의존. topic 미분류(58%) 문항은 본문 n-gram에만 의존.
